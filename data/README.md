# Data Directory

This directory is intentionally empty in GitHub except for placeholders.

Run:

```bash
python scripts/00_download_geo.py
```

to download processed GEO `.h5ad` files into `data/raw_geo/`.

Generated metadata files will be written to `data/metadata/`.

